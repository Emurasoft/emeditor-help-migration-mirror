[package]
name = "emeditor-help-migration-mirror"
version = "0.1.0"
edition = "2021"
authors = ["Makoto Emura <makoto@emurasoft.com>"]

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = { version = "0.7", features = ['http'] }

[lints.clippy]
pedantic = "warn"